The following extra packages are installed for this project:

  sudo apt-get install pep8 pylint3 python3-coverage python3-nose python3-websockets

The specific versions in use are:

  python3 version (3.5.1-3)
  python3-nose version (1.3.7-1).
  pep8 version (1.7.0-2).
  pylint3 version (1.5.2-1ubuntu1).
  python3-coverage version (3.7.1+dfsg.1-1ubuntu7).
  python3-websockets version (3.0-1).


start the engine with:

  cd source
  make server


start all the code validation and then the engine with:

  cd source
  make
